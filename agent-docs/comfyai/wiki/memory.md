# Memory — Distilled Cross-Session Knowledge

> This file is for things that have **proven true across multiple sessions** — not fresh observations from today.
> Add here sparingly. If something has only happened once, put it in `sessions/` first.
> Graduate it here only once it's held up over time.

---

## How to use this file

- **Read:** At session start if `index.md` doesn't have enough context.
- **Write:** When something from `sessions/` is clearly stable and broadly useful.
- **Don't write:** Per-session events, one-off errors, or anything you're still figuring out.

Split large sections into sub-pages (`memory/models.md`, `memory/nodes.md`, etc.) if this grows unwieldy.

---

## User — Working Style & Preferences

[Stable, confirmed observations about how this user likes to work.
Things confirmed across sessions, not just assumed after one conversation.]

---

## Environment — Stable Facts

[Hardware, model configuration, server setup, known resource limits.
Things unlikely to change between sessions.]

---

## Node & Workflow Knowledge

[Cross-workflow observations about specific nodes — how they behave, what breaks them, what they pair well with.
Only graduate from sessions/ once confirmed on multiple workflows.]

---

## Recurring Patterns

[Things that come up repeatedly — error types, prompt approaches, workflow structures that reliably work or fail.]

---

## Open Questions (cross-session)

[Things still being figured out across multiple sessions. Once resolved, move the answer up to the relevant section.]

- [ ] 
