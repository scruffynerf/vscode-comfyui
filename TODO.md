# TODO

Prioritized implementation backlog. Items are roughly ordered by: usefulness to agents/users, implementation difficulty, and dependency on other items.

---

## Tier 1 — High value, low complexity

_(All Tier 1 items done, targeting 2.1.0 — see CHANGELOG for details.)_

---

## Tier 2 — High value, moderate complexity

_(All Tier 2 items done, targeting 2.1.0 — see CHANGELOG for details.)_

---

## Tier 3 — Significant value, higher complexity or dependency

- IN PROGRESS **Node knowledge library**: Port and extend the VibeComfy node registry concept — a curated, queryable reference of node types, custom node packs, common patterns, and usage examples. Intended as a retrieval source for agents building or modifying workflows.
  - ✅ Node catalog (index.md, per-class files, node-registry.json) — done, targeting 2.1.0
  - ✅ appmana pip catalog (2559 packages with summaries, local copy at `comfyai/nodes/appmana-catalog.json`, searchable by keyword)
  - ✅ `find-a-node.md` — step-by-step discovery hierarchy; Step 2 leads with capability index
  - ✅ `install-custom-nodes.md` — UV install patterns with safety note (plain pip + extra-index-url vulnerability)
  - ✅ Popular nodes list (`popular-nodes.md`) — top 100 from Comfy Registry, sorted by category, appmana cross-referenced
  - ✅ Capability index — router (`capability-index.md`) + per-bucket detail files (`capability/<slug>.md`)
      - Node class names shown per pack, all matched nodes (no truncation), count hint `*(N nodes)*`
      - git-only packs flagged inline with link to install guide; install block moved below the list
      - Word-boundary CamelCase matching; `~prefix` for compound keywords (controlnet, ipadapter, etc.)
      - Exclusion rules prevent generic buckets swamping specialised ones (Video→Text, Audio→File, etc.)
      - `tools/node-name-transforms.json` — per-pack `strip_prefix` and `strip_suffix` for noisy class names
      - `tools/rebuild-docs.sh` — single command to regenerate all docs (`--refresh` to re-fetch API)
      - Curation files: `tools/popular-nodes-veto.json`, `popular-nodes-includes.json`, `popular-nodes-descriptions.json`
      - Maintenance: `tools/release-prep.md`
  - ✅ **find-a-node.md Step 3 — direct search fallbacks**
      - A: `registry-search.json` (655KB, ~2600 packs) — keyword search on id/name/desc; generated by `tools/generate_search_indexes.py`
      - B: `node-class-search.json` (540KB, ~1700 packs) — class-name search, pack-centric format; same generator
      - C: `https://comfyregistry.org` surfaced to user as last resort
  - ⬜ Populate curation files with hand-curated entries (ongoing):
      - `popular-nodes-descriptions.json` — more agent-friendly description overrides (was-ns, impact-pack, etc.)
      - `popular-nodes-veto.json` — review remaining duplicates/deprecated packs
      - `popular-nodes-includes.json` — must-include packs that fall below rank 100
      - `node-name-transforms.json` — prefix/suffix strips for noisy node class names (ongoing)
  - ⬜ Common workflow patterns / usage examples (the VibeComfy knowledge base port — `knowledge_base.md` is a stub)

- **Workflow diffing**: Semantic diff between two workflow JSONs — show what nodes/links were added, removed, or changed, in human-readable form. Useful for reviewing agent changes and understanding iteration history.

---

