# Common Patterns

Successful node combinations discovered through experimentation. Each file documents one pattern.

See `quick-ref.md` for workflow selection guidance.

---

## Patterns

| Pattern | When to use |
|---------|-------------|
| [[lighting-portraits]] | Portrait lighting setups |
| [[quick-to-quality]] | Z-Turbo → Portrait Gen progression |
| [[image-to-image-flows]] | Flux edit patterns |

---

## Adding a pattern

When you discover a successful node combination or a prompt that works well:

1. Create a file in `wiki/patterns/` with a descriptive name
2. Include:
   - **Context** — what were you trying to achieve?
   - **Nodes used** — exact node types and configuration (or prompt text for prompt patterns)
   - **Why it works** — your hypothesis
   - **When to use** — when to prefer this pattern vs alternatives
   - **Limitations** — what it doesn't do well

3. Update this index to link the new pattern
